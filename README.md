# PlayPark Admin Control

Static frontend for the PlayPark Desktop Bot administration portal.

The browser bundle contains only the Supabase project URL and publishable key. Administrative data remains protected by Supabase Auth, RLS, and admin RPC checks.
